mutation deletePayment($checkNumber: String, $customerNumber: Int!){
    deletePayment(checkNumber: $checkNumber, customerNumber: $customerNumber){
        amount
        id{
            checkNumber
            customerNumber
        }
        paymentDate
    }
}